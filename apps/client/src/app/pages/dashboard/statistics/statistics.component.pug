.chart-wrapper.pt-3.pb-5(*ngIf="statistics && values")
  ngx-charts-line-chart(
      [results]="values",
      [xAxis]="true",
      [yAxis]="true",
      [showGridLines]="true",
      [roundDomains]="true",
      [animations]="false",
      [scheme]="colorScheme",
  )
