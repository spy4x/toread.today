import { GetIconByItemTypePipe } from './get-icon-by-item-type.pipe';

describe('GetIconByItemTypePipe', () => {
  it('create an instance', () => {
    const pipe = new GetIconByItemTypePipe();
    expect(pipe).toBeTruthy();
  });
});
