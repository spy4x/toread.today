import { TagTitleByIdPipe } from './tag-title-by-id.pipe';

describe('TagTitleByIdPipe', () => {
  it('create an instance', () => {
    const pipe = new TagTitleByIdPipe();
    expect(pipe).toBeTruthy();
  });
});
