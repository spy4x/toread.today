import { TagsByIdsPipe } from './tags-by-ids.pipe';

describe('TagsByIdsPipe', () => {
  it('create an instance', () => {
    const pipe = new TagsByIdsPipe();
    expect(pipe).toBeTruthy();
  });
});
